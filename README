INSTALATION
===========

type:
./configure
make
make install

Note.: The compilation is qmake based, not automake/autoconf based.

If you use Fedora or Suse and see a error message like

"*** No rule to make target `/usr/qt/3/mkspecs/linux-g++/qmake.conf', needed by `Makefile'. Stop"

Copy the file "/usr/lib/qt3/mkspecs/linux-g++/qmake.conf" to "/usr/qt/3/mkspecs/linux-g++/" and try again


RUNING
======

To run knetstats, type:
knetstats


CONTACT
=======
Any doubts or suggestions, hugo_pl@users.sourceforge.net

For more info. visit: http://knetstats.sf.net


UNINSTALL
=========

Type:
rm -f /usr/bin/knetstats
rm -rdf /usr/share/apps/knetstats/*
rm -f /usr/share/applications/knetstats.desktop
rm -f $HOME/.kde/share/config/knetstats.rc

--
Hugo P.L.
